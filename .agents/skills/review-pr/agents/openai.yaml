interface:
  display_name: "Review PR"
  short_description: "Review a GitHub PR by number with gh"
  default_prompt: "Use $review-pr to fetch a GitHub PR by number, analyze it thoroughly, return findings in chat, and post the review with gh CLI."
